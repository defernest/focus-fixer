# 📹 Focus-Fixer

<div style="text-align:center"><img src="img/readme.jpg" /></div>

Утилита, для простого и автоматического контроля фокуса камер AXIS. Умеет работать и как сервис, регулярно выставляя автофокусировку на камерах, и как CLI-утилита ручного конфигурирования.

## 🔽 Установка

Сросто скопируйте бинарный файл для вашей ОС на сервер.  
Например:

`rsync --progress -avzhe ssh /path/to/focusfix 192.168.0.4:/usr/bin/focusfix`

## 🧾 Приступая к работе

Перед работой необходимо составить конфигурационный файл со списком камер AXIS в следующем формате:  

|IP          |LOGIN|PASSWORD|
|------------|-----|--------|
|10.20.150.10|root |admin   |

```csv
ip,login,pass
10.20.150.10,root,admin
...
```
**Обязательна первая строка `ip,login,pass`!**  
Далее любое кол-во камер с доступами.  

**На всех камерах для работы необходимо установить `Basic Auth`, как способ авторизации!** Иначе утилита выдаст `401 HTTP Error` для камер с иным типом авторизации.

## ♾️ Режим сервиса

`focusfix bot -t 1 -c config.csv` - где `[--timeout] -t 1` - время в минутах, между итерациями проходов по списку камер; `[--config] -c <file_name.cvs>` - путь к конфигурационному файлу со списком камер.

Для `[--timeout] -t 1` можно указывать нужный формат продолжительности постфиксами `1m | 1h | 1d`. При указании часов или дней, произойдет конвертация.

### Пример простого .service-файла для Debian-based систем

`$ sudo touch /etc/systemd/system/focusfix-daemon.service`
`$ sudo chmod 664 /etc/systemd/system/focusfix-daemon.service`

```ini
[Unit]
Description="FocusFix is an easy way to focus your cameras"
Requires=network.target
After=network.target

[Service]
Type=simple
ExecStart=/usr/bin/focusfix -t 1 -c config.csv
Restart=always
RestartSec=60

[Install]
WantedBy=multi-user.target
```

`$ sudo systemctl daemon-reload`
`$ sudo systemctl start focusfix-daemon`

### У меня windows-сервер

[Страдайте!](https://nssm.cc/) А лучше пройдите исправительные работы (не менее 180 часов конфигурирования unix-based систем) и навсегда примите [Tux](https://ru.wikipedia.org/wiki/Tux) как единственного Бога!

---

В режиме сервиса утилита будет бесконечно проходить по спику камер, отправляя запросы на автофокусировку и ожидая ответа в течении 1 минуты. Если в течении 1 минуты ответ не поступит, соединение будет разорванно, а в лог будет отданна ошибка таймаута.

Если ответ поступит, его код и результат будут обработанны. Отсутствие вывода в лог говорит о успешном автофокусе камеры. Иначе выдается соответствующая ошибка. Т.е. **результат работы контролируется**.

Таймаут между проходами по списку камер работает следующим образом:

```txt
***запуск сервиса***
начало цикла
проход по всему списку камер
таймаут
окончание цикла
...
***окончание работы сервиса***
```

Результаты работы, а также ошибки возникшие при работе с камерами будут записаны в системный журнал и доступны к просмотру следующей командой:  
`journalctl -u <service_name>` - где `<service_name>` - имя сервиса

## ✋ Ручной режим

### Linux

`focusfix fix -c config.csv` - где `[--config] -c <file_name.cvs>` - путь к конфигурационному файлу со списком камер.

### Windows

`.\focusfix fix -c=".\config.csv"` - где `[--config] -c="<file_name.csv>"` - путь к конфигурационному файлу со списком камер.

Результаты работы будут представлены в выводе консоли (std), при возникновении ошибок, подробное описание проблемы, с указанием IP (если проблема касается камеры и/или доступа к ней) и пр. параметров будет выведено в консоль по завершению работы по списку. **В стандартном режиме, результат работы является контролируемым.**

В результате успешной работы, будет выдано сообщение с кол-вом успешно настроенных камер.

## ℹ️ Помощь

Вызовите `focusfix help` или `focusfix [cmd] help` (доступна короткая версия команды `-h`) для быстрого вызова справки.  
Дополнительные вопросы, предложения и комментарии можно задать в [📲 Telegram](https://t.me/defernest).